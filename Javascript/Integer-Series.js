'use strict'

//Write a function that accepts two arguments (start, end) and generates an array containing integers


function generateIntegers(start, end) {
	var container = [];
	for(let curNum = start;curNum<=end;curNum++) {
		container.push(x);	
	}
	return container;
}


